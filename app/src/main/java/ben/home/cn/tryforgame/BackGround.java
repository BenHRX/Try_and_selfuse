// package ben.home.cn.tryforgame;

/**
 * Created by benhuang on 17-8-27.
 */

/* public class BackGround extends Spirit {


    public BackGround(Bitmap bitmap) {
        super(bitmap);
        this.getSpeed().set_velx(0);
        this.getSpeed().set_vely(0);
        this.getSpeed().set_velxDirection(1);
        this.getSpeed().set_velyDirection(1);
        this.getCoordinates().set_x(0);
        this.getCoordinates().set_y(0);
    }

}                                               采用继承Spirit类并不能把Bitmap都获得，因为Bitmap是私有成员
                                                所以直接将BackGround设置为一个类，背景用特殊处理*/
